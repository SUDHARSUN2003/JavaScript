/*
function toTopperCase(name) {
    return name.charAt(0).toUpperCase()+name.slice(1);
}
*/

/*
const toTopperCase = function(name)
{
    return name.charAt(0).toUpperCase()+name.slice(1);
}
*/

const toTopperCase = (name) =>
    {
        return name.charAt(0).toUpperCase()+name.slice(1);
    }

 console.log(toTopperCase("sudharsun"));

